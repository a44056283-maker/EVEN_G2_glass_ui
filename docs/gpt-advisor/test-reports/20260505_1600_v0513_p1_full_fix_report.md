# v0.5.13 P1 Full Fix Report

Generated: 2026-05-05 16:00

## Status

```text
P0 CORE TRUE_DEVICE PASSED
P1 DEEP STABILIZE: IN PROGRESS
v0.5.13: TEST BUILD
Release status: NOT APPROVED FOR STORE RELEASE
```

This build continues the v0.5.11 takeover package work. It is a test build for real-device validation, not a formal release candidate.

## Subagent Work

- crash-log-agent: completed crash logging and audit report.
- location-vision-agent: completed location context config/API wiring.
- photo-preview-history-agent: completed IndexedDB-first history store and thumbnail rendering.
- vision-accuracy-agent: completed prompt/context enhancement and low-confidence review path.
- trading-cache-focus-agent: completed read-only audit; patch integrated in main rollout.

## 7 Issues

| # | Issue | Code Status | Real-device Status |
|---|-------|-------------|--------------------|
| 1 | Glass crash | Runtime logging added: `window.onerror`, `unhandledrejection`, `safeGlassShow`, backend JSONL log | Pending 3-round device run |
| 2 | Location vision | Setting added; 5s timeout + 5min cache; failure non-blocking; prompt receives coarse context | Pending permission/device run |
| 3 | Photo preview/history | G2 shows captured/recognizing text; phone history supports thumbnail/summary | Pending camera run |
| 4 | Empty phone history | IndexedDB primary, localStorage fallback, memory fallback, visible storage error | Pending WebView persistence run |
| 5 | Vision accuracy | VLM prompt includes question/time/location/recent history; low-confidence review path | Pending visual quality run |
| 6 | Trading slow/inaccurate submenu | 15s cache TTL, stale display, background refresh, loading words | Pending R1 trading run |
| 7 | Trading R1 focus | Trading branch locks focus to submenu; detail click force-refreshes current tab | Pending R1 trading run |

## Verification

Passed:

```bash
node ./node_modules/typescript/bin/tsc --noEmit -p apps/evenhub-plugin/tsconfig.json
node ./node_modules/typescript/bin/tsc -p packages/shared/tsconfig.json --noEmit
node ./node_modules/typescript/bin/tsc -p packages/vision-adapter/tsconfig.json --noEmit
node ./node_modules/typescript/bin/tsc -p services/api-server/tsconfig.json --noEmit
npm --workspace apps/evenhub-plugin run build
npm --workspace apps/evenhub-plugin run pack
```

## EHPK

- Version: `0.5.13`
- Size: `83557 bytes`
- SHA256: `e5a074e35524c7b180732aebef3ef7dc8edb02c62b237de75f8daf6855f9b1a3`
- Project path: `apps/evenhub-plugin/g2-vision-voice-assistant.ehpk`
- Desktop backup: `/Users/luxiangnan/Desktop/g2-vision-voice-assistant-v0.5.13-p1-test.ehpk`

## Manual Acceptance Matrix

Required but not yet executed in this Codex session:

1. Start glass app and keep it open for 5 minutes.
2. Visual recognition 5 times.
3. Cancel photo 2 times.
4. Location-aware visual recognition 3 times.
5. Photo history save 3 times.
6. Voice-triggered vision 3 times.
7. Trading whitelist price page 3 times.
8. Trading label switching 3 times.
9. OpenCLAW memory Q&A 2 times.
10. History refresh 2 times.

This must be repeated for 3 real-device rounds before store release.

## Known Remaining Blocker

OpenCLAW auth keeps `OPENCLAW_GATEWAY_TOKEN ?? OPENCLAW_TOKEN` precedence. The remote gateway can still return relay timeout/no-answer. The API marks generic no-answer as `openclaw:unavailable` to avoid false success. A real OpenCLAW memory Q&A still requires gateway agent relay repair/verification.

## Store Decision

Do not upload as official store release yet. Use v0.5.13 only for P1 real-device validation.
