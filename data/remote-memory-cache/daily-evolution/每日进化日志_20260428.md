# 每日进化日志 2026-04-28

## 今日主题

L5 进化实验室启动: 补齐回测与数据订单薄。

## 已完成

- 创建 L5 专业机构级进化计划。
- 9099 首页新增 `L5进化实验室` 书签，位置在 M4 后面。
- 删除首页 `决策采集汇总` 书签入口。
- 新增 L5 页面: `/tabs/l5-evolution.html`。
- 新增 L5 市场微结构接口:
  - `/api/l5/market_intel?pair=all`
  - `/api/l5/backtest_orderbook_status`
- 接入数据源:
  - Binance Futures 订单薄/OI/爆仓
  - OKX SWAP 订单薄/OI/爆仓
  - Gate Futures 订单薄/OI
  - Alternative.me Crypto Fear & Greed Index
- 固化新增数据模块规范: 全部采用三级缓存加载。
  - L1 内存热缓存。
  - L2 本地 JSON/SQLite 缓存。
  - L3 实时公网数据源。
- L5 增加决策证据链:
  - 天眼AI入场评测记录绑定 L5 市场微结构快照。
  - 出山AI出场评测记录绑定 L5 市场微结构快照。
  - 新增 L5 快照审计与参数候选池接口。
- L5 场景话术与 AI 提示词接入:
  - 新增 L5 市场微结构场景: 订单薄倾斜、点差、OI覆盖、爆仓流、恐惧贪婪。
  - 天眼AI入场/持仓审计 prompt 已加载 L5 场景，但 L5 不单独触发下单。
  - 出山AI出场 prompt 已加载 L5 场景，用于保护利润、滑点风控和反转猎杀观察。
  - 天眼/出山英雄卡接口已返回 `l5_data` 与 `l5_scenario`。
- L5 回放与归因闭环补齐:
  - 新增 `/api/l5/replay_samples`，把天眼/出山决策记录物化为 M1-M5 可回放样本。
  - 新增 `/api/l5/param_registry`，同步 V6.5 实盘参数只读注册表。
  - 新增 `/api/l5/daily_attribution`，统计样本来源、缓存层级和数据源降级情况。
  - L5 页面新增回放样本数、参数注册数、24H 归因展示。
- Freqtrade 原版架构对齐审计:
  - 已识别真实原版目录 `/Volumes/TianLu_Storage/Freqtrade源代码_from_B`。
  - 当前 `bt_tools/freqtrade_installed` 已包含 exchange、optimize、rpc、persistence、strategy、data、tests、user_data 核心目录。
- 运行验证:
  - 9099 已重启并加载新接口。
  - L5 快照: 689 条。
  - L5 回放样本池: 2000 条。
  - 24H 归因样本: 1674 条，其中天眼 1000 条、出山 674 条。
  - V6.5 参数注册表: 104 项。
  - 12 个执行代理已注册: 8081-8084、9090-9097，9094 已在注册列表中。

## 当前判断

这些数据源对现行交易系统有用:

- 订单薄: 判断点差、盘口倾斜、短线滑点风险。
- OI: 判断趋势是否有机构加仓或减仓。
- 爆仓: 辅助 M3 反转猎杀和出山AI出场优先级。
- 贪婪指数: 作为大盘情绪风控，不直接下单，只调节置信度和杠杆。

## 明日建议

- 把 L5 回放样本与真实订单盈亏做胜率、滑点、提前平仓归因。
- 对 L5 single 级订单薄/OI 源补强到三所交叉。
- 参数候选必须先跑回测和前测，不直接写入实盘配置。

## 6小时定时巡检 08:41:23

- 报告: `/Users/luxiangnan/Desktop/每日进化日志/定时巡检/scheduled_evolution_20260428_084123.md`
- 机器人注册: 12 / 12
- 异常命令端口: []
- 问题数: 3
- 主要问题:
  - [medium] l5_market_intel: L5存在降级/单源覆盖: {'single': 10, 'quiet': 5}
  - [medium] l5_market_intel: L5存在降级/单源覆盖: {'single': 10, 'quiet': 5}
  - [medium] l5_market_intel: L5存在降级/单源覆盖: {'single': 10, 'quiet': 5}

## 常态化定时任务

- 已新增 6 小时自动巡检/回测任务: `com.tianlu.scheduled-evolution-6h`。
- 执行周期: 每 21600 秒自动运行一次。
- 脚本: `/Users/luxiangnan/freqtrade_console/scripts/scheduled_evolution_6h.py`。
- LaunchAgent: `/Users/luxiangnan/Library/LaunchAgents/com.tianlu.scheduled-evolution-6h.plist`。
- 每轮执行内容:
  - 9099 控制台健康检查与安全拉起。
  - 12 个机器人执行代理健康检查、命令端口检查、注册修复。
  - M1-M5 统一数据流、L5 市场微结构、每日归因、机器人状态三轮轮测。
  - L5 回放样本刷新与 V6.5 参数注册表刷新。
  - BT2 轻量回测启动与状态轮询。
  - 输出 JSON/Markdown 报告到 `/Users/luxiangnan/Desktop/每日进化日志/定时巡检/`。
- 巡检首跑发现并修复:
  - 天眼入场扫描 L5 批量获取异常: `'dict' object has no attribute 'ThreadPoolExecutor'`。
  - 原因: 后端部分 L5 并发调用依赖全局 `futures` 名称，运行期可能被污染。
  - 修复: L5 快照、天眼持仓 L5、天眼入场 L5、AI数据流 L5 改为局部 `concurrent.futures` 导入。
- 当前保留风险:
  - L5 订单薄/OI 仍存在 single 级覆盖，需要继续补强三所交叉数据源。

## 回测模块完工提醒

- 已写入 L5 进化计划: 回测模块作为接下来主线任务。
- 预计节奏:
  - 2天: 稳定可用版，修 BT2 轮询、结果目录、1970时间异常、9099展示。
  - 3-5天: 交易闭环版，打通 L5 回放样本、真实订单盈亏、滑点、提前平仓、V6.5 参数候选池。
  - 7-10天: 机构雏形版，加入 walk-forward、分市场状态、订单生命周期、回测/实盘一致性审计。
- 下一次工作提醒:
  - 优先修复 BT2 状态轮询超时。
  - 统一 `results` 与 `reports` 目录。
  - 修复 `1970 exit_time`。
  - 把 L5 样本与真实订单盈亏归因打通。

## 三省六部交易辅助中枢改造 09:54

- 服务: `http://127.0.0.1:7891/`
- 新页面: `http://127.0.0.1:7891/trading-aux`
- 文档: `/Users/luxiangnan/edict/docs/三省六部交易辅助中枢改造_20260428.md`
- 已补齐三块缺失能力:
  - 事件总线: `/api/trading/event-bus/status`、`/api/trading/events`
  - 组合风控: `/api/trading/portfolio-risk`
  - 研究实验管理: `/api/research/experiments`
- 数据落盘:
  - `/Users/luxiangnan/edict/data/trading_event_bus.jsonl`
  - `/Users/luxiangnan/edict/data/portfolio_risk_snapshot.json`
  - `/Users/luxiangnan/edict/data/research_experiments.json`
  - `/Users/luxiangnan/edict/data/trading_aux_config.json`
- 首轮验证:
  - 7891 `/healthz` 正常。
  - 组合风控返回 `medium` 风险，主要告警为 Gate.io 敞口集中度偏高。
  - 事件总线已写入 `risk.portfolio`、`market.event`、`research.experiment`。
  - 已创建首个研究实验: `V6.5 M1-M4 15M共振实验`。
- 额外修复:
  - 7891 `/healthz` 原有 `os.WEEK` 拼写错误修复为 `os.W_OK`，避免健康检查空响应。

## 三省六部与9099自动驾驶桥接 10:02

- 已把 9099 控制台接入 7891 事件总线和组合风控。
- 天眼AI入场执行前新增组合风控门:
  - `medium/normal` 默认放行。
  - `high` 或风险分较高时要求更高置信度。
  - `critical/frozen` 或同交易对集中度红灯时拦截入场。
- 已接入事件:
  - `ai.tianyan`: 决策接收、执行结果、风控拦截。
  - `ai.chushan`: 出山AI非 hold/critical/已执行评测。
  - `robot.order`: 机器人命令下发、结果、异常。
  - `risk.portfolio`: 入场前组合风险检查。
- 新增 9099 自检接口:
  - `GET /api/edict/bridge_status`
  - `POST /api/edict/bridge_smoke`
- 验证结果:
  - `python3 -m py_compile console_server.py` 通过。
  - 9099 `/api/ports_status` 返回 12 个机器人在线。
  - 9099 `/api/edict/bridge_status` 正常读取 7891 风控与事件总线。
  - 9099 `/api/edict/bridge_smoke` 已写入 7891 `market.event / bridge.smoke`。
- 注意:
  - 本轮只做无订单副作用验证，没有手动触发实盘入场或平仓。

## 出山AI执行链路断点修复 10:09

- 审计问题:
  - 7891 事件总线出现 `ai.chushan / chushan.evaluation.result`。
  - 典型事件: `de_action=full_close`、`executed=false`、`execution_result=None`。
- 根因:
  - 9099 自动驾驶总开关为关闭状态: `/api/autopilot/status enabled=false`。
  - DecisionEngine 给出了出场动作，但执行权限未打开，因此没有下发机器人命令。
- 已修复:
  - 9099 `/api/edict/bridge_status` 增加自动驾驶状态，桥接检查可以直接看到是否有执行权限。
  - 出山AI事件增加:
    - `autopilot_enabled`
    - `execution_blocker`
    - 明确的 `execution_result`
  - 当自动驾驶关闭时，事件显示 `execution_blocker=autopilot_disabled`，不再留下空结果。
- 额外风控修复:
  - 原规则会把 `R2健康分=0` 单独判定为“濒死强制全平”。
  - 该规则与“普通浮亏不由出山AI抢平，交由天眼/V6.5判断方向错误”的逻辑冲突。
  - 已改为: R2低分只能作为风险信号，只有接近爆仓或深度止损时才触发保护性全平/半平。
- 验证:
  - `python3 -m py_compile console_server.py` 通过。
  - 9099 重启正常。
  - 12 个机器人在线。
  - 新事件已正确写入 `execution_blocker=autopilot_disabled`。
  - R2规则修复后，新一轮出山评测未再把普通浮亏的 `R2=0` 推成 `full_close`。

## 自动驾驶启用前预检 10:20

- 继续修复:
  - 发现旧进程仍在监听 9099，导致旧版 R2 强平话术继续写入历史。
  - 已改为通过 9099 监听端口 PID 精确重启，确认新进程在修复后启动。
- 总闸修复:
  - 自动驾驶关闭时，出山AI扫描不再走旧版 `execute_now` 自动执行分支。
  - 旧版门控命中时只记录 `execution_blocker=autopilot_disabled_legacy_gate`，不下发机器人命令。
  - 事件总线会记录被总闸拦截的旧门控信号，方便审计。
- 新增只读预检接口:
  - `GET /api/autopilot/preflight`
  - 检查项:
    - 12个机器人在线。
    - 7891三省六部桥接可用。
    - 组合风控非 critical/frozen。
    - 当前9099进程启动后无旧版 `R2健康分=0濒死，强制全平` 残留。
    - 自动驾驶关闭时无机器人命令下发。
- 验证结果:
  - `ready_for_enable=true`。
  - `robots_online=12/12`。
  - `risk_level=medium score=10`。
  - `dispatch_count=0`。
  - 当前进程窗口内 `legacy_r2_full_close=0`。

## 三省六部WEB UI同步升级 10:28

- 问题:
  - 7891 后端能力已改造，但首页 React 看板没有入口，用户看到的三省六部 WEB 页面没有明显变化。
  - `/trading-aux` 深度页存在，但未展示 9099 自动驾驶预检和完整数据工作流。
- 已实现:
  - 首页新增 `交易中枢` 书签。
  - 新增 React 面板 `TradingAuxPanel`，展示:
    - 三省六部实时数据工作流。
    - 自动驾驶启用前预检。
    - 组合风控 KPI、告警、敞口分布。
    - 事件总线实时事件。
    - 钦天监研究实验队列。
  - 7891 新增同源代理接口:
    - `GET /api/trading/autopilot-preflight`
    - 由 7891 代理读取 9099 `/api/autopilot/preflight`，避免浏览器跨端口 CORS 问题。
  - `/trading-aux` 深度页同步加入:
    - 真实数据工作流。
    - 自动驾驶启用前预检。
    - 敞口条形分布。
- 构建与重启:
  - `npm run build` 成功，已生成新版 dist。
  - 7891 已重启，当前监听 `127.0.0.1:7891`。
- 验证:
  - 首页加载新版 `index-XqKkImIu.js`。
  - 构建产物包含 `交易中枢`、`三省六部实时数据工作流`、`/api/trading/autopilot-preflight`。
  - `/trading-aux` 页面包含 `真实数据工作流`、`自动驾驶启用前预检`。
  - `/api/trading/autopilot-preflight` 返回 `ready_for_enable=true`，代理标记为 `7891_to_9099`。
  - Playwright 未安装，未做浏览器截图；已完成 HTTP 与构建产物验证。

## 三省六部预检安全闸收紧 10:32

- 发现:
  - 组合风控整体为 `medium` 时，仍可能存在单项 high/critical 风险告警。
  - 当前样本中出现 BTC/USDT 仓位集中度 high 告警，如果只看整体等级，容易误判为可以开启自动驾驶。
- 已修复:
  - 9099 `/api/autopilot/preflight` 新增 `no_high_risk_alarms` 检查。
  - 任意 high/critical 告警存在时，`ready_for_enable=false`。
  - 7891 `交易中枢` 与 `/trading-aux` 会通过同源代理展示该拦截结果。
- 验证:
  - 9099 预检返回 `high_or_critical_alarm_count=1`。
  - 9099 预检返回 `ready_for_enable=false`。
  - 7891 代理返回 `proxy=7891_to_9099` 且同步显示 `ready_for_enable=false`。
  - 7891 与 9099 服务均已重启并监听正常。

## 三省六部进入天眼/出山AI输入链路 10:37

- 下一步已完成:
  - 9099 新增三省六部只读 AI 上下文构建。
  - 上下文来源包含:
    - 7891 事件总线最近事件。
    - 兵部组合风控快照与 high/critical 告警。
    - 钦天监研究实验与回测晋级门槛。
  - 新增 9099 只读接口:
    - `GET /api/edict/context?pair=BTC/USDT&role=smoke`
- 已注入的 AI 链路:
  - 天眼AI持仓审计 prompt。
  - 天眼AI入场信号 prompt。
  - 天眼AI批量持仓 prompt。
  - 出山AI出场/保护 prompt。
- 权限边界:
  - 三省六部只提供事件、组合风控和研究约束。
  - 最终入场仍由天眼AI决策。
  - 最终出场仍由出山AI决策。
  - 机器人只执行授权命令，三省六部不直接抢下单权。
- 当前实测:
  - `/api/edict/context` 能返回 BTC/USDT 仓位集中度 high 告警。
  - 上下文显示 `high_or_critical_alarms=1`。
  - 上下文显示研究实验 `V6.5 M1-M4 15M共振实验` 及“三轮回测 + 一轮小资金前测 + 兵部风控无红灯”晋级门槛。
  - `/api/edict/bridge_status` 返回 `ok=true`。
  - 9099 与 7891 预检链路正常，自动驾驶仍为关闭状态。

## 三省六部导航书签精简 10:45

- 问题:
  - 7891 首页顶部书签过多，高频工作入口和低频配置/档案入口混在一起。
- 已调整:
  - 外层保留高频入口:
    - 旨意看板
    - 交易中枢
    - 朝堂议政
    - 省部调度
    - 动态干预
  - 低频入口归入 `更多` 折叠菜单:
    - 档案与任务: 小任务、奏折阁、旨库、天下要闻。
    - 系统配置: 官员总览、模型配置、技能配置。
    - 低频交易页: 机器人。
- 验证:
  - 前端 `npm run build` 通过。
  - 7891 首页已加载新版 `index-B1kCa7rX.js` 与 `index-BfTa-g_V.css`。
  - 构建产物包含 `更多`、`档案与任务`、`低频交易页`、`交易中枢`。

## L5订单薄/OI三级缓存与三所交叉修复 11:01

- 今日顺序任务:
  - 补齐回测与数据订单薄。
  - 先处理 L5 市场微结构数据源稳定性。
- 审计发现:
  - `/api/l5/market_intel?pair=all` 可返回 5 个白名单币。
  - 但 Binance/OKX 直连在当前网络下超时，导致订单薄和 OI 降级为 `single`。
  - `/api/v1/ai_data_flow` 冷启动早期可能短暂返回 M1 `NO_DATA/ratio=0` 或 L5 `warming` 占位。
- 已修复:
  - L5 公网请求改为多路获取:
    - direct 直连。
    - 5020 本地代理。
    - 5020 认证代理。
  - L5 订单薄/OI 缓存健康度收紧:
    - `orderbook/open_interest` 至少 dual 才作为健康缓存。
    - single/missing 磁盘缓存不再阻止实时刷新。
  - `/api/v1/ai_data_flow` 改为英雄卡安全数据流:
    - 遇到 L5 warming/missing 时同步刷新真实数据。
    - 遇到 M1 `NO_DATA + ratio=0` 时触发同步刷新。
  - 统一数据流冷启动时如果 M1 缺失，主动触发一次 M1 扫描补齐。
- 验证:
  - `/api/l5/market_intel?pair=all` 三轮测试:
    - BTC/ETH/SOL/BNB/DOGE 订单薄全部 `triple 3`。
    - BTC/ETH/SOL/BNB/DOGE OI 全部 `triple 3`。
    - 恐惧贪婪指数返回 `33 Fear`。
  - `/api/v1/ai_data_flow` 三轮测试:
    - 5 个交易对 L5 均为 `orderbook=triple`、`oi=triple`。
    - M1 已恢复真实 15M 数据，示例: DOGE 量比 3.142x、BTC 2.307x。
  - `/api/v1/ai_data_flow/audit?rounds=3` 返回:
    - `ok=true`
    - `issue_count=0`
    - 三轮均无核心字段缺失。
  - 9099 已重启并监听正常。
- 下一步:
  - 继续把 L5 样本与真实订单盈亏做胜率、滑点、提前平仓归因。
  - 参数候选仍必须先跑回测和前测，不直接写入实盘配置。

## L5样本真实订单归因补齐 11:17

- 今日继续任务:
  - 将 L5 回放样本与真实机器人订单/持仓结果对齐。
  - 生成胜率、PnL、滑点、提前平仓归因。
- 审计发现:
  - `l5_replay_samples` 已有 2117 条样本。
  - `l5_snapshots` 已有 1870 条快照。
  - 旧归因接口只统计样本数和数据源降级，没有对齐真实订单盈亏。
  - `trade_journal.json` 与部分 MACB/OKX 机器人 trade_id 不完全一致，单靠 trade_id 匹配会得到 0。
- 已修复:
  - `/api/l5/daily_attribution` 新增 `trade_outcome_attribution`。
  - 增加真实订单匹配:
    - 先按 `port + trade_id + pair` 精确匹配。
    - 再按 `port + pair + direction + entry_price` 宽松匹配。
    - 同时合并 `trade_journal.json` 与 12 个机器人只读状态/交易历史。
  - 新增指标:
    - `matched_trade_count`
    - `open_trade_count`
    - `closed_trade_count`
    - `avg_unrealized_pnl_pct`
    - `avg_realized_pnl_pct`
    - `win_rate_pct`
    - `avg_slippage_pct`
    - `premature_exit_count`
    - `by_pair`
    - `by_source`
    - `slippage_samples`
    - `premature_samples`
- 三轮验证:
  - 最近24小时样本数: 1791。
  - 已匹配真实订单/持仓: 1200。
  - 当前匹配样本仍以持仓中为主: open=1200, closed=0。
  - 平均未实现盈亏约 -1.79% 到 -1.80%。
  - 平均滑点估算约 -0.18302%。
  - 当前未发现符合规则的疑似提前平仓样本。
- 注意:
  - `validation_levels` 中仍有历史 single 快照，来自修复前旧快照；当前 L5 实时数据已恢复 triple。
  - 后续需要继续提高历史订单同步质量，让 closed 样本进入归因统计。

## L5真实订单归因WEB UI同步 11:22

- 用户要求:
  - 新增数据模块不能只做接口和数据对接，必须同步补齐对应 WEB 页面展示。
- 已修复:
  - 9099 控制台 `L5 进化实验室` 页面新增 `真实订单归因` 展示区。
  - 页面已接入 `/api/l5/daily_attribution?hours=24` 的 `trade_outcome_attribution` 字段。
  - 新增可视化指标:
    - 匹配真实订单数。
    - 持仓订单数。
    - 已平订单数。
    - 未实现均值。
    - 已实现均值。
    - 胜率。
    - 平均滑点。
    - 疑似提前平仓数。
    - 三所校验与缓存覆盖。
  - 新增明细展示:
    - 按天眼/出山来源拆分的归因表。
    - 按交易对拆分的归因表。
    - 滑点样本列表。
    - 提前平仓/未匹配订单风险样本列表。
- 验证:
  - 9099 静态页面已能返回 `真实订单归因`、`匹配真实订单`、`平均滑点`、`疑似提前平仓` 等页面字段。
  - 页面 JS 通过 `node --check` 语法检查。
  - 当前接口样本:
    - `matched=1200`
    - `open=1200`
    - `closed=0`
    - `avg_slippage_pct=-0.18302`
    - `premature=0`

## BT2回测目录统一与异常时间修复 11:32

- 今日继续任务:
  - 推进回测模块稳定化。
  - 让回测结果能被 L5 页面稳定读取和展示。
- 审计发现:
  - BT2真实结果写在 `/Volumes/TianLu_Storage/bt_tools/backtest_core/reports`。
  - 9099 的 L5 状态接口原来读取 `/Users/luxiangnan/freqtrade_console/bt_tools/backtest_core/results`，导致页面显示 `暂无回测结果`。
  - `bt_all_pairs.json` 中有 6 条已平仓交易的 `exit_time` 显示为 `1970-01-01`。
- 已修复:
  - 新增 BT2 统一结果目录读取:
    - `/Volumes/TianLu_Storage/bt_tools/backtest_core/reports`
    - `/Users/luxiangnan/freqtrade_console/bt_tools/backtest_core/reports`
    - `/Users/luxiangnan/freqtrade_console/bt_tools/backtest_core/results`
    - `/Users/luxiangnan/freqtrade_console/bt_tools/reports`
  - `/api/bt2/history` 改为统一目录读取，并返回 `result_dirs`。
  - `/api/bt2/results/<id>` 增加异常出场时间修复:
    - 当 `exit_time` 年份小于 2000 时，用 `entry_time + holding_minutes` 计算真实展示时间。
    - 标记 `exit_time_repaired=true` 与 `exit_time_source=entry_time + holding_minutes`。
  - `/api/bt2/overview` 返回的 `latest_result` 已清洗，不再包含 `1970` 出场时间。
  - `/api/l5/backtest_orderbook_status` 已显示真实最新回测文件、结果目录、交易数、胜率和异常时间修复数量。
  - 9099 `L5进化实验室` WEB 页面新增回测摘要 UI:
    - 最新结果。
    - 历史结果数。
    - 总交易。
    - 胜率/总PnL。
    - 异常时间修复计数。
- 验证:
  - `/api/bt2/history` 返回:
    - 最新文件 `bt_all_pairs.json`
    - 总交易 `6`
    - 胜率 `33.3%`
    - `exit_time_repaired_count=6`
  - `/api/bt2/results/all_pairs` 返回:
    - trades `10`
    - `1970` 时间数量 `0`
    - repaired `6`
  - `/api/bt2/overview` 返回:
    - last_backtest `2026-04-21 13:54`
    - total_trades `6`
    - winrate `33.3%`
    - avg_pnl `45.41%`
    - latest_result 中不再包含 `1970`
  - `/api/l5/backtest_orderbook_status` 返回:
    - latest_result `bt_all_pairs.json`
    - result_count_sample `1`
    - total_trades `6`
    - winrate `33.3%`
    - exit_time_repaired_count `6`
  - 9099 已重启并监听正常，PID `79463`。

## 6小时定时巡检 20:02:54

- 报告: `/Users/luxiangnan/Desktop/每日进化日志/定时巡检/scheduled_evolution_20260428_200254.md`
- 机器人注册: 12 / 12
- 异常命令端口: []
- 问题数: 2
- 回测提醒: 2天稳定版: 修BT2轮询、结果目录、1970时间异常、9099展示。
- 主要问题:
  - [medium] replay_samples: HTTP Error 500: INTERNAL SERVER ERROR
  - [medium] bt2_run: Command '['/Library/Frameworks/Python.framework/Versions/3.14/Resources/Python.app/Contents/MacOS/Python', '/Users/luxiangnan/freqtrade_console/bt_tools/backtest_core/run_backtest.py', '--pair', 'BTC/USDT', '--exchange', 'gate', '--output', '/Users/luxiangnan/freqtrade_console/bt_tools/backtest_core/reports/bt_dc6b8234.json', '--days', '30']' timed out after 90 seconds
