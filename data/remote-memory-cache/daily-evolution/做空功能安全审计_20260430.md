# 做空功能安全审计_20260430

审计时间: 2026-04-30 12:39 CST

## 结论

当前本机 9090-9097 交易机器人具备做空能力,未发现 FOttStrategy 老旧原生策略正在生效。

做空链路状态:

- Freqtrade 配置: futures + isolated。
- 9090-9097: `short_allowed=true`。
- 当前运行策略: `FOttStrategy`。
- 当前 FOttStrategy: V99 纯 API 对接层,不产生自主 `enter_long/enter_short`。
- 当前 FOttStrategy: `can_short=True`。
- 当前 FOttStrategy: `position_adjustment_enable=False`。
- 9099 天眼执行链路支持 `EXECUTE_SHORT` → `/api/v1/forceenter` with `side=short`。
- 历史数据库存在真实 `is_short=1` 交易记录,证明交易所/API层面做空不是空话术。

## 已修复风险

### 1. 天眼提示词残留冲突条款

发现:

- `console_server.py` 中存在一句提示词: “只要活的M1-M5数据流强烈共振,即便V6.5未触发,也可EXECUTE”。
- 这与 V6.5 硬门控优先原则冲突,存在 AI 绕过 V6.5 的风险。

处理:

- 已改为: 活的 M1-M5 只能增强或否决 V6.5 已放行候选; V6.5 未触发硬门控时禁止 `EXECUTE_LONG/EXECUTE_SHORT`。
- `python3 -m py_compile console_server.py` 已通过。
- 已重启 `com.tianlu.console-server`, 9099 健康接口恢复正常。

### 2. 9093 杠杆配置不一致

发现:

- 9093 overlay 仍为 `long=5, short=5`。
- 其他 9090-9097 已是 `long=10, short=10`。

处理:

- 已将 `/Users/luxiangnan/freqtrade/config_9093_overlay.json` 同步为 `long=10, short=10`。
- 当前 9093 进程如需完全加载 overlay 文件,需在合适窗口重启 9093; 天眼执行链路仍会在 `/forceenter` 请求中显式传入目标杠杆。

## 未发现的问题

- 未发现 `can_short=False`。
- 未发现 `short_allowed=false`。
- 未发现活跃 FOttStrategy 中存在原生开仓规则。
- 未发现活跃 FOttStrategy 中存在 `position_adjustment_enable=True`。
- 未发现 9090-9097 被配置为 spot。
- 未发现做空方向在 9099 执行链路中被映射成做多。

## 限制

- 8081-8084 本机 `127.0.0.1` 不可达,应在电脑B侧继续审计。
- 本次没有执行真实做空下单测试,只做接口与配置审计,避免产生实盘风险。

## 建议

- 电脑B 8081-8084 需要同步跑同一套检查。
- 9093 可在无关键持仓或低风险窗口重启,让 overlay 杠杆配置完全进入运行态。
- 保留 FOttStrategy V99 纯 API 层,继续禁止任何原生交易规则恢复。

