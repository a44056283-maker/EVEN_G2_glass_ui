# 9095 OKX Bot 掉线故障

**日期**: 2026-04-22
**现象**: 9095进程消失，端口9095无监听
**影响**: OKX Bot 9095 无法交易

---

## 症状

```
lsof -i :9095  # 无输出，端口未监听
ps aux | grep 9095 | grep freqtrade  # 无进程
```

日志最后输出：
```
2026-04-22 16:11:30,111 - freqtrade - ERROR - Fatal exception!
sqlalchemy.exc.PendingRollbackError: This Session's transaction has been rolled back
due to a previous exception during flush. Original exception was:
  sqlite3.IntegrityError: UNIQUE constraint failed: orders.ft_pair, orders.order_id
```

---

## 根本原因

1. OKX WebSocket 多次断线重连（`Connection reset by peer`）
2. 断线重连期间，OKX 交易所重发了订单事件
3. Bot 重复插入了同一 `order_id=3500926441828458496`
4. SQLite UNIQUE 约束触发IntegrityError
5. 事务回滚后 SQLAlchemy session 卡死在 `PendingRollbackError` 状态
6. Bot 无法继续任何操作，最终 Fatal exception 退出

---

## 修复步骤

### 1. 检查数据库（确认无残留重复）

```python
import sqlite3
db = "/Users/luxiangnan/freqtrade/user_data_okx_9095/tradesv3_okx.sqlite"
conn = sqlite3.connect(db)
cur = conn.cursor()
cur.execute("""
    SELECT ft_pair, order_id, COUNT(*) as cnt
    FROM orders
    GROUP BY ft_pair, order_id
    HAVING cnt > 1
""")
dups = cur.fetchall()
print(f"重复订单: {len(dups)}")  # 应为0
```

### 2. 重启机器人

```bash
cd /Users/luxiangnan/freqtrade
SSL_CERT_FILE=/Volumes/TianLu_Storage/certs/ca.crt \
FREQTRADE_ROOT=/Users/luxiangnan/freqtrade \
HTTP_PROXY=http://admin:Xy%4006130822@127.0.0.1:5020 \
HTTPS_PROXY=http://admin:Xy%4006130822@127.0.0.1:5020 \
ALL_PROXY=http://admin:Xy%4006130822@127.0.0.1:5020 \
AIOHTTP_DISABLE_HTTP2=1 \
nohup .venv311/bin/python3 .venv311/bin/freqtrade trade \
  --config config_shared.json \
  --config config_9095_overlay.json \
  --userdir /Users/luxiangnan/freqtrade/user_data_okx_9095 \
  -s FOttStrategy > /private/tmp/bot_9095.log 2>&1 &
```

### 3. 验证

```bash
lsof -i :9095 | grep LISTEN  # 应有输出
curl -s -u "admin:Xy@06130822" http://127.0.0.1:9095/api/v1/ping  # {"status":"pong"}
```

---

## 预防建议

OKX WebSocket 断线频繁可能导致重复订单问题：
- 检查本地VPN代理（5020端口）稳定性
- 检查SSH隧道是否稳定：`sshpass -p 'Xy@06130822' ssh -N -L 5020:127.0.0.1:3128 root@8.222.178.87`
