## User Profile

The user maintains a local file-based memory system for agents and runs recurring operational workflows inside `/Users/luxiangnan/.openclaw/workspace-tianlu`, especially cron-style automation, Feishu reporting, end-of-day summaries, and trading-system refinement. In this memory set, the strongest recurring expectation is operational verification: for scheduled runs, they care about whether the named script actually produced or refreshed the on-disk artifact and whether the result was written back into the day's memory log, not just whether a command launched or exited cleanly. For trading-system refinement, they want V6.5 preserved as the hard-decision layer and want AI demoted to auditor/administrator rather than autonomous trader. For end-of-day summaries, they tend to provide the exact output frame up front and expect direct execution against that contract. Good collaboration for this workspace means restoring local context first when expected, staying close to the real file paths, checking on-disk state with concrete file evidence, and preserving exact retrieval handles such as script names, output files, sections, flags, and error strings. In this workspace there is also one explicit wording rule from `SOUL.md`: call the father “爸” or “父亲”, never “大哥”.

## User preferences

- For cron-style automation runs, verify the written file/state and the memory append; do not stop at the script exit code.
- For trading-system refinement, preserve the user's framing `我们原来的V6.5规则本身没有问题,而是让AI去全权负责`; default to protecting V6.5 and tightening signal quality rather than making AI more discretionary.
- When the user says `让AI成为真正的管理员和审核员,而不是乱平乱入场`, treat AI as auditor/gatekeeper, not as a free-form trader with autonomous entry/exit authority.
- When stabilizing the trading machine after freezes, keep `VPN` / `TODESK` / `OPENCLAW` / `VSCODE` running and stop unrelated apps first; `TODESK是远程软件不关`.
- When the user asks “你现在是什么模型？”, answer from live session state directly instead of giving a long explanation or a guess.
- When the user asks whether a service or plugin is “正常吗”, treat that as an end-to-end health check: public endpoint, local listener, config, and logs together, not just one layer.
- When the user says “能修复一下吗？我这会儿眼镜访问不到open claw了。”, treat it as a live repair request; change the broken route and verify the real reply path instead of only explaining backend health.
- During long live restarts or verifications, keep short status updates flowing; silence long enough to trigger “你怎么了？” is already a collaboration failure for this workflow.
- For `workspace-tianlu` 晨间复盘, follow the fixed order `错误日志 -> 昨日复盘 -> 周计划 -> 审批 -> 交易日志`; don’t re-ask for structure once that pattern is named.
- For the same morning-review workflow, use the exact short frame `昨日复盘 / 未改进错误 / 今日学习主题 / 待处理事项` instead of a long narrative summary.
- When the morning review asks whether 户部 has `重大风险`, actively compare sentiment (`做空/极度恐慌`) with live position direction, leverage, and liquidation distance instead of reporting those sources separately.
- When the cron task is explicitly named `天禄-外部信号自动获取(P2)`, treat the expected unit of work as “抓取 + 写回当日总结”, not just running the fetcher.
- When the task is phrased as “外部信号自动获取(P2)”, check data-source reachability, fallback behavior, and the latest persisted file state by default.
- For routine cron completions with `alerts=[]`, default to execute + verify + quiet completion; do not over-narrate normal success or escalate without an anomaly.
- For this cron family, keep the durable order `run script -> inspect external_signals.json -> update daily memory`; do not hide the bookkeeping step in the middle.
- When proving a cron run really refreshed output, use direct file evidence such as `stat`/mtime on `Knowledge/external_signals/external_signals.json`, not only console success text.
- For the `external_signals` cron family, the strongest proven verification pattern is `jq` on `Knowledge/external_signals/external_signals.json` plus `stat` for size/mtime.
- For the same `external_signals` workflow, prefer `jq '{fetch_time, funding_rate, long_short_ratio, fear_greed, alerts}'` or `jq 'keys'` over old flat-field probes; the validated top-level shape is `fetch_time / funding_rate / long_short_ratio / fear_greed / alerts`.
- For the same `external_signals` workflow, if a broad `jq` expression gets brittle, fall back to `external_signals_fetcher.py --status` plus targeted reads of `.funding_rate`, `.long_short_ratio`, `.fear_greed`, and `.alerts`; do not mix old fields like `fear_greed_index` into one pipeline and then treat the resulting error as a fetch failure.
- If an `external_signals` `jq` probe returns `null`, inspect `jq 'keys'` first and then read the actual saved paths such as `.fear_greed`; do not assume older names like `fear_greed_index`.
- A lighter but still proven verification path for the same cron family is `python3 -m json.tool Knowledge/external_signals/external_signals.json` plus `stat` when the goal is artifact validation rather than deep field inspection.
- Another proven lightweight check for the same cron family is `stat` plus a quick content read such as `sed` when the goal is “did the file refresh and still contain the expected signal fields?”
- For repeated `external_signals` runs in this environment, expect mixed upstream health: Binance may still return `No route to host`, but one field can recover while another still falls back. Check the saved per-field `exchange` and `source_note` values before labeling the whole run failed or recovered.
- For the freshest `external_signals` reruns, expect the daily-memory append to land under `memory/YYYY-MM-DD.md` but not always under the same heading; nearby successful runs used `## 外部信号`, the external-signals section, and `## Heartbeat`, and both the `19:24` and `21:12` runs still needed a manual backfill after the JSON refresh, so verify the actual section instead of assuming a fixed heading or auto-append.
- If `external_signals_fetcher.py` does not finish immediately, do not assume success from launch; poll or re-check `Knowledge/external_signals/external_signals.json` mtime and parsed fields after the process exits.
- If an `external_signals` fetch session still looks in-flight but `--status`, `python3 -m json.tool`, and `stat` already prove the saved file refreshed, trust the persisted artifact over ambiguous process-state checks.
- When a cron message already names the script to run, default to direct execution-and-verify workflow rather than planning discussion.
- When a cron task says “Return your response as plain text; it will be delivered automatically.”, keep the result plain text and avoid markdown framing.
- In `workspace-tianlu` scheduled jobs, start by restoring context from `SOUL.md`, `USER.md`, and the current `memory/YYYY-MM-DD.md` before running the named script.
- For TianLu reminder runs governed by `HEARTBEAT.md`, keep routine reminders internal: if approvals are clear and nothing needs attention, default to `HEARTBEAT_OK` instead of a user-facing update.
- When the reminder policy says “Do not relay it to the user unless explicitly requested.”, do not surface routine reminder output unless the reminder explicitly requires user delivery.
- When a reminder says “Do not infer or repeat old tasks from prior chats,” keep the run self-contained and do not pull in unrelated historical context.
- When reporting this cron family back, keep the status concise and data-first around `Binance`, `Gate`, `alerts`, and file freshness.
- When the user specifies `发送飞书到：oc_5016041d5ee6ed2a8cc4e98372569cec`, treat Feishu delivery as part of task completion rather than optional follow-up text.
- For merged晨报 runs, if the instruction says “如某项失败则静默跳过”, omit only the failed subtask and still send the combined report.
- When a task gives the fixed title frame `📋 三省六部晨报（日期）` plus the Feishu target `oc_5016041d5ee6ed2a8cc4e98372569cec`, send directly in that format instead of re-confirming layout or recipient.
- For end-of-day TianLu summaries, if the task says `今日总结（200字内）+ 明日首要任务`, keep the output short, direct, and conclusion-first.
- For the same end-of-day summary workflow, treat `写入 memory/YYYY-MM-DD.md` plus `更新 self_evolution_2026_q1.md` as one completion contract and verify both writes.
- When asked to “整理今日学习心得”, turn the latest learning notes into report-ready takeaways instead of only listing file titles.
- If today’s `memory/learning/daily_learning_YYYY-MM-DD.md` is missing at report time, fall back to the newest prior learning note and mark today as `待执行/待补充` instead of pretending the same-day note exists.
- If the user supplies the exact watchdog command `bash /Users/luxiangnan/edict/scripts/cloudflared-watchdog.sh`, execute that exact path rather than inferring another entrypoint.
- For routine watchdog heartbeat checks, follow the process expectation to “只在异常时打扰” / “only disturb on abnormal”: do the silent check first and escalate only for non-zero exit codes or reported disconnects.
- For watchdog and similar maintenance runs, the durable completion rule is “写回今天的记忆，再做一次落盘校验”; do not stop after the shell check if the daily note has not been re-read from disk.
- For healthy watchdog/log-maintenance runs, keep the edit narrow: “只追加这次 watchdog 的实际结果，避免改动其他流水”.
- When watchdog verification needs to be stronger than “exit code 0 + no disconnects”, use log/state evidence: recount the last-hour disconnects from `com.cloudflare.cloudflared.err.log` and confirm `launchctl print gui/$(id -u)/com.cloudflare.cloudflared` is still running.
- When a watchdog run is healthy, keep the response short and avoid extra investigation.
- For the `门下省` daily backlog check, preserve the explicit rule “静默执行，禁止回复 HEARTBEAT_OK”; silent completion is the default unless there is an anomaly.
- When a cron workflow already has a fixed time and Feishu target, such as the 08:55 `门下省` check to `oc_5016041d5ee6ed2a8cc4e98372569cec`, execute directly instead of re-confirming parameters.
- When the `门下省` check finds `state=="中书省"` count `0`, still send the empty-result Feishu notice and write the daily memory line.
- For the `门下省` daily review, recalculate from today’s `/Users/luxiangnan/edict/data/tasks_source.json` every time; do not inherit yesterday’s `中书省=0` result.
- When replying in this workspace, preserve the explicit naming rule from `SOUL.md`: call the father “爸” or “父亲”, never “大哥”.
- Keep concrete retrieval handles intact in memory artifacts: exact script names, file paths, flags, section names, and error strings like `external_signals_fetcher.py`, `external_signals.json`, `cloudflared-watchdog.sh`, `auto_processor.py`, `## 中书省`, `--status`, `No route to host`, and `无待处理任务`.
- For repeated `external_signals` failures, preserve the exact Binance host/error handles when they appear, such as `fapi.binance.com`, `www.binance.com`, and `[Errno 65] No route to host`, because they are stable retrieval clues for this environment.
- For repeated `external_signals` failures, also preserve newer Binance TLS/error handles like `SSLEOFError` and `EOF occurred in violation of protocol` when they appear; they are actionable retrieval clues, not noise.
- If a scheduled processor returns no tasks, still write the timestamped daily record; “no-op” is not a reason to skip the day log.
- When the cron wrapper says the result can be delivered automatically if no direct message is sent, finish the run and return a concise completion note instead of adding extra callback choreography.

## General Tips

- In this memory folder, `raw_memories.md` is the routing layer; use it first to find thread metadata, then deepen into the matching rollout summary when you need richer evidence.
- `trade_journal.json` is the fastest way to summarize current account state in this workspace: read `stats` plus `open_positions` to get PnL, win rate, leverage, and `dist_to_liquidation_pct` without rebuilding the ledger manually.
- For live system health checks, the best source order is `http://127.0.0.1:9099/health` / `/health/readiness` / `/api/dashboard` / `/api/dashboard/positions` / `/api/ports_status`, then per-port `/api/v1/status` and `/api/v1/profit`, then `trade_journal.json` and the sentiment files for reconciliation.
- For OpenClaw host/service diagnostics, keep the source order concrete: `session_status` for the current model, `uptime` / `pmset -g batt` / `vm_stat` / `df -h` / `top` / `memory_pressure` for Mac health, then public URL + local listener + config + logs for bridge/plugin checks.
- For Even G2 / OcuClaw repair work, check `~/.openclaw/even-ai-settings.json` early; healthy `even2026` domain responses and healthy plugin config do not rule out a broken runtime model route.
- When console totals and `trade_journal.json` disagree, prefer the live `9099` API for current system-wide state and use `trade_journal.json` as a drill-down artifact.
- For `workspace-tianlu` morning reviews, the fastest high-signal set is `memory/error_log.md`, yesterday’s `memory/YYYY-MM-DD.md`, `memory/learning/self_evolution_2026_q1.md`, `/Users/luxiangnan/edict/data/pending_approvals.json`, and `/Users/luxiangnan/freqtrade_console/trade_journal.json`; that path is cheaper than wide repo searching.
- The approvals file for this workflow is at `/Users/luxiangnan/edict/data/pending_approvals.json`, not under the workspace root.
- For `workspace-tianlu` external-signal runs, validate completion with `python3 Knowledge/external_signals/external_signals_fetcher.py --status` and by checking `Knowledge/external_signals/external_signals.json`; if the process is still running, re-check after exit instead of closing on launch.
- If the task needs stronger proof of refresh, check `jq` output plus `stat -f '%Sm %z %N' -t '%Y-%m-%d %H:%M:%S %Z' Knowledge/external_signals/external_signals.json` to confirm both content and mtime/size changed.
- For the same `external_signals` workflow, treat `funding_rate.exchange` and `long_short_ratio.exchange` as separate health signals; a new verified run restored Binance funding while BTC long/short still used Gate fallback, so avoid collapsing source status into one binary summary.
- For the same `external_signals` workflow, prefer the newest append time plus on-disk file proof when summarizing current health; the freshest rerun now anchors on the `09:09` writeback in `memory/2026-05-01.md`, where `external_signals_fetcher.py --status`, `stat`, `jq`, and the daily-memory line matched after local context was restored first, and the saved state kept funding rate on Binance at `0.0089%` while `long_short_ratio.exchange` still fell back to Gate at `1.01`, `fear_greed.value = 26`, and `alerts=[]`. The adjacent `08:57`, `08:50`, `08:42`, `08:36`, `08:25`, `08:19`, and `08:09` runs remain the nearby richer-proof branches because they added the same mixed-source validation with `python3 -m json.tool`; `07:55`, `07:49`, `07:42`, `07:37`, `07:25`, `07:18`, `07:12`, `07:05`, `06:55`, `06:48`, `06:42`, `06:37`, `06:25`, `06:22`, `06:17`, `06:12`, `06:05`, `05:55`, `05:48`, `05:42`, `05:35`, `05:25`, `05:22`, `05:18`, and `05:12` keep the same freshness shield: when the preexisting file is already only a few minutes old, compare the pre-run and post-run mtime instead of assuming the current cron wrote it. The `09:09` run adds the clearest current proof that the durable order is still “restore context -> run -> verify artifact -> append day log -> re-read the line”, while `07:49` remains the clearest nearby branch where the JSON was already fresh but the task still stayed open until the missing memory line was backfilled, which re-confirmed that artifact proof and memory proof are separate gates.
- For the same `external_signals` workflow, use the file's real top-level keys when probing with `jq`: `alerts`, `fear_greed`, `fetch_time`, `funding_rate`, and `long_short_ratio`; do not assume `fear_greed_index` or `market_sentiment` exists at top level.
- If a field-level `jq` check comes back `null`, treat that as a schema-check trigger rather than a data failure; `jq 'keys'` is the fastest recovery step.
- In zsh, do not use `status` as a scratch variable in combined verification commands; it is read-only, so prefer `exit_code`.
- Treat fetched values and `memory/YYYY-MM-DD.md` append targets as time-specific; reuse the procedure, not the numbers.
- If Binance is unreachable but Gate fallback produced a populated output file, do not treat that as a blocker.
- If a daily-memory patch fails because the expected context drifted, reread the file tail or target section before retrying `apply_patch`.
- For healthy `cloudflared-watchdog.sh` runs, the lightweight proof is not enough by itself: pair the clean script result with `launchctl print gui/$(id -u)/com.cloudflare.cloudflared` and a memory append in the active daily section. Verify the actual section each time, because late-day rows used both `## 工部` and `## Cloudflared Watchdog`; the newest clean anchor is `09:05 定时看门狗执行完成`, the adjacent `08:46` and `08:33` runs remain the nearby metrics/log-recount branches, and the strongest deeper-proof branch remains the adjacent `06:47` run with `bash -n` still passing, the active metrics endpoint confirmed at `127.0.0.1:20241/metrics`, and the service still running under `launchctl`.
- If a watchdog check needs deeper proof, do not assume `/tmp/cloudflared-watchdog.state` should exist; that file is only written on alert-threshold runs, so use log recount plus `launchctl` instead.
- For `tianlu-evolution` daily self-reflection, the proven entrypoint is `python3 /Users/luxiangnan/.openclaw/skills/tianlu-evolution/scripts/periodic_nudge.py daily`; do not assume `BOOTSTRAP.md` exists in `workspace-tianlu`.
- For `tianlu-evolution` monthly self-reflection, the proven entrypoint is `python3 /Users/luxiangnan/.openclaw/skills/tianlu-evolution/scripts/periodic_nudge.py monthly`; verify `memory/monthly-YYYY-MM.md` on disk and keep the delivered result plain text when the cron says it will be auto-delivered.
- In the `auto_processor.py` maintenance workflow, success can mean `{"processed": 0, "tasks": []}` plus a daily-memory append; do not require downstream task execution when the queue is empty.
- For the `门下省` backlog cron, the fastest first check is `jq '[.[] | select(.state=="中书省")] | length' /Users/luxiangnan/edict/data/tasks_source.json`; if it is `0`, skip list-building and go straight to empty-result notification plus memory append.
- For the merged `三省六部晨报` workflow, the efficient pattern is parallel execution of `taizi_triage.py patrol` plus the 中书省/尚书省 `auto_report.py report` commands, then one merged Feishu send and one append under `## 三省六部晨报` in `memory/YYYY-MM-DD.md`.
- In repeated `workspace-tianlu` cron runs, `rg` was unavailable (`zsh:1: command not found: rg`); default to `grep` / `find` / `sed` instead of spending time retrying ripgrep-specific workflows.
- In TianLu reminder workflows, the fast routine is `HEARTBEAT.md` -> approvals check -> run the named script -> verify `memory/YYYY-MM-DD.md`; do not over-reason once policy and approvals say nothing needs attention.
- In `/Users/luxiangnan/edict/data/pending_approvals.json`, historical `rejected` proposals are not live work; distinguish them from actual `pending` items before escalating a heartbeat run.
- For qintianjian news-crawler verification, parse `/Users/luxiangnan/edict/data/sentiment_pool.json` as `d.get('news', {}).get('items', [])`; `news` is not a flat top-level list.

## What's in Memory

### /Users/luxiangnan/.openclaw/workspace-tianlu

#### 2026-05-01

- 晨间复盘与风险提示: morning review, 晨间复盘, memory/error_log.md, self_evolution_2026_q1.md, pending_approvals.json, trade_journal.json, 做空/极度恐慌, sentiment mismatch, 仓位降噪
  - desc: Fixed-order morning-review memory for `cwd=/Users/luxiangnan/.openclaw/workspace-tianlu`, covering how to restore context from the current error log, yesterday’s memory, week plan, approvals file, and 户部 trade journal, then output the short frame `昨日复盘 / 未改进错误 / 今日学习主题 / 待处理事项`. Search this first when a task says `晨间复盘`, asks for today’s learning focus, or asks whether the current positions carry `重大风险`.
  - learnings: The freshest run re-confirmed the cheapest authoritative path: `/Users/luxiangnan/edict/data/pending_approvals.json` is the approvals truth source, `/Users/luxiangnan/freqtrade_console/trade_journal.json` is the 户部 source, and the review should not stop at sentiment alone. The useful delta was the conflict check: `.sentiment_latest.json` pointed to `做空/极度恐慌`, but live positions still included an ETH 3x long while the 9092 BTC 5x short sat closest to the liquidation warning line, so position-direction consistency and `min_liq_distance` belong in the morning output by default.

- external_signals automation mixed-source verification: external_signals_fetcher.py, external_signals.json, funding_rate.exchange = binance, long_short_ratio.exchange = gate, Gate fallback, source_note = binance_unreachable_fallback, gate_user_count_ratio, jq '{fetch_time, funding_rate, long_short_ratio, fear_greed, alerts}', jq, python3 -m json.tool, 09:37 外部信号自动获取(P2)执行完成, 09:22 外部信号自动获取(P2)执行完成, memory/2026-05-01.md
  - desc: Cron-style P2 external-signal fetch verification for `cwd=/Users/luxiangnan/.openclaw/workspace-tianlu`, covering how to rerun the fetcher, prove `Knowledge/external_signals/external_signals.json` refreshed, distinguish Binance funding recovery from Gate long/short fallback, and confirm the same run was written back into `memory/2026-05-01.md`. Search this first when a task mentions `外部信号自动获取(P2)`, `external_signals_fetcher.py`, or asks whether the newest saved signal really landed.
  - learnings: The freshest anchors are now the `09:37`, `09:22`, and `09:18` writebacks in `memory/2026-05-01.md`, where `external_signals_fetcher.py --status`, `stat`, `jq`, and the daily-memory lines matched and the saved state kept the same mixed-source pattern: funding rate `0.0090%` at `09:37`, `0.0009%` at `09:22`, and `0.0094133%` at `09:18` from Binance while long/short ratio stayed on Gate fallback around `0.99-1.00`, `fear_greed.value` held at `26`, and `alerts=[]`. The new `09:37` run sharpened the routing clue again: artifact refresh and daily-memory writeback are still separate gates even on a clean mixed-source success, so re-read the new line after patching instead of assuming the fetcher logged it. The `09:09`, `08:57`, and `08:50` runs remain the nearest earlier mixed-source anchors, `08:42` remains the nearby branch with the extra final `python3 -m json.tool` gate, `08:36` remains the nearby branch that proves launch success is not enough and process completion plus mtime movement still matter, `08:25` remains the nearby branch where the JSON refresh succeeded but today’s memory still lagged, `08:19` remains the nearby branch that keeps the same per-field source split even when funding-rate recovery on Binance looks clean, `08:09` remains the nearby branch with the extra `python3 -m json.tool` proof, `07:55` remains the nearby branch with the extra final `jq -e` proof, and `07:49` remains the clearest nearby branch where the JSON was already fresh at `2026-05-01 07:49:52 CST 1589 bytes` but the task still stayed open until the missing memory line was backfilled. The adjacent `07:42`, `07:37`, `07:25`, `07:18`, `07:12`, `07:05`, `06:55`, `06:48`, `06:42`, `06:37`, `06:25`, `06:22`, `06:17`, `06:12`, `06:05`, `05:55`, `05:48`, `05:42`, `05:35`, `05:25`, `05:22`, `05:18`, and `05:12` runs kept the same per-field source split and showed that the important distinction is still per-field source status, not a binary “Binance up/down” label. The same evidence also re-confirmed the freshness shield sharpened at `05:25` and re-tested through the later runs: if a preexisting file is only a couple of minutes old, do not assume the current cron already refreshed it; compare pre-run and post-run mtime before closing. The `06:42` run still matters as the clearest recent duplicate-heading failure branch because the first append created a duplicate `## 外部信号` heading and the task only closed after the line was moved back under the existing section.

- 门下省 empty-result review and Feishu delivery: 门下省, 中书省, tasks_source.json, oc_5016041d5ee6ed2a8cc4e98372569cec, 飞书, 静默执行, 禁止回复 HEARTBEAT_OK, 超过4小时, memory/2026-05-01.md
  - desc: Scheduled 门下省 review memory for `cwd=/Users/luxiangnan/.openclaw/workspace-tianlu`, covering how to recompute the current `tasks_source.json` state, send the Feishu result to the fixed chat target even when the backlog is empty, and append the same outcome into `memory/2026-05-01.md`. Search this first when a task mentions `门下省`, `中书省`, the fixed Feishu chat `oc_5016041d5ee6ed2a8cc4e98372569cec`, or a silent empty-result review.
  - learnings: The freshest run rechecked `/Users/luxiangnan/edict/data/tasks_source.json` instead of inheriting yesterday’s answer, confirmed `Taizi=180` and `中书省=0`, sent the empty-result Feishu notice successfully, and then wrote the same result into `memory/2026-05-01.md`. The durable routing clue is that “empty queue” does not mean “no side effects”: this workflow still expects outbound notification and the day log update, but it remains silent in chat because the rule is `静默执行，禁止回复 HEARTBEAT_OK`.

- cloudflared watchdog maintenance: cloudflared-watchdog.sh, cloudflared-watchdog, heartbeat, EXIT_CODE=0, 近1h断线次数: 0, 09:34 定时看门狗执行完成, 09:16 定时看门狗执行完成, launchctl print gui/$(id -u)/com.cloudflare.cloudflared, 127.0.0.1:20241/metrics, /health, /api/health, /api/v1/ping, memory/2026-05-01.md
  - desc: Routine watchdog heartbeat/cron checks for `cwd=/Users/luxiangnan/.openclaw/workspace-tianlu`, including the exact script path `/Users/luxiangnan/edict/scripts/cloudflared-watchdog.sh`, live `launchctl` verification, and the required daily-memory append for the active day file. Search this first when a task mentions `cloudflared-watchdog.sh`, a silent watchdog heartbeat, or asks whether the newest watchdog run actually got logged.
  - learnings: The freshest anchor moved to `09:34` on `2026-05-01`: the script still returned `[看门狗] 检查完成. 近1h断线次数: 0`, `launchctl` still showed PID `1047` in `state = running`, `http://127.0.0.1:20241/metrics` stayed reachable, public `https://console.tianlu2026.org/` still returned HTTP 200, and the local app probes now resolved cleanly at `127.0.0.1:9099/health` and `/api/health` while the older `/api/v1/ping` path returned `404`; the task only counted as complete after the `09:34 定时看门狗执行完成` line was re-read from `memory/2026-05-01.md`. The nearby `09:16`, `09:05`, `08:46`, `08:33`, `08:16`, and `08:05` runs remain useful for routing nuances: endpoint drift does not invalidate a healthy watchdog check here, a stale `cloudflared.err.log` is weaker evidence than the live trio of process + metrics + public root URL, and a bad shell pipeline can produce a fake Python `SyntaxError` if raw `/metrics` output is piped into `python -`. The adjacent `06:47` run remains the strongest deeper-proof branch because `bash -n` still passed there and a quick port probe re-confirmed the live metrics endpoint on `127.0.0.1:20241/metrics` when a remembered port was wrong and `lsof` was unavailable. The adjacent `07:46`, `07:33`, `06:34`, `06:16`, `06:03`, `05:46`, `05:33`, `05:16`, `05:03`, `04:46`, `04:33`, `04:16`, `04:03`, `03:46`, `03:33`, and `03:16` runs reinforce the same closure rule: even after a clean script run and healthy `launchctl`, completion still depends on re-reading the appended line with `grep` and `stat`; if the section already exists, verify the exact timestamped line rather than trusting script output or file mtime alone; if the section does not exist yet, create `## Cloudflared Watchdog` first and then re-check the append.

- cron daily memory maintenance with `auto_processor.py`: auto_processor.py, 中书省, 无待处理任务, processed=0, tasks_source.json, Taizi: 180, 中书省: 0, 08:11 旨意自动处理器定时执行完成, memory/2026-05-01.md
  - desc: Cron-style no-pending-task maintenance for `cwd=/Users/luxiangnan/.openclaw/workspace-tianlu` where `/Users/luxiangnan/.openclaw/workspace-zhongshu/scripts/auto_processor.py` runs cleanly, the queue is expected to be empty, `tasks_source.json` is rechecked, and the result is still written into `memory/2026-05-01.md`. Search this first when the processor says `无待处理任务` and you need the full completion contract rather than just stdout.
  - learnings: The freshest no-op anchor is `08:11`: `auto_processor.py` still returned `{"processed": 0, "tasks": []}`, but the run was only fully verified after `/Users/luxiangnan/edict/data/tasks_source.json` was rechecked as `total=180, 中书省=0` and the `08:11 旨意自动处理器定时执行完成` line was appended under `## 中书省` in `memory/2026-05-01.md`.

- 三省六部 merged晨报: 三省六部晨报, taizi_triage.py patrol, auto_report.py report, workspace-taizi, workspace-zhongshu, workspace-shangshu, oc_5016041d5ee6ed2a8cc4e98372569cec, 如某项失败则静默跳过, messageId, memory/2026-05-01.md
  - desc: Scheduled merged晨报 workflow for `cwd=/Users/luxiangnan/.openclaw/workspace-tianlu`, covering the parallel 太子/中书省/尚书省 checks, the fixed `📋 三省六部晨报（日期）` output frame, the Feishu send to `oc_5016041d5ee6ed2a8cc4e98372569cec`, and the append under `## 三省六部晨报` in `memory/2026-05-01.md`. Search this first when a task mentions `三省六部晨报`, the fixed Feishu chat, or asks for one merged morning status report instead of separate subsystem checks.
  - learnings: The represented run showed the efficient merge shape clearly: run `taizi_triage.py patrol` plus the two `auto_report.py report` commands in parallel, keep successful results even if one substep fails, send one merged Feishu message, then append the same summary and `messageId=om_x100b500bfa16acb0b268cbcf4b6f282` to `memory/2026-05-01.md`. The useful retrieval clue is that the source outputs are already summary-friendly: 太子 emits `监控Agent数: 5`, `钦天监 ... OK`, and `Bot-9090 ... Bot-9097 存活`, while 中书省/尚书省 both returned `执行中 0 条、待派发 0 条` with `Taizi=180`.

- monthly self-reflection nudge: 每月自省, periodic_nudge.py monthly, tianlu-evolution, Return your response as plain text; it will be delivered automatically, memory/monthly-2026-05.md, 活跃 Skills：0 个, 本月无重大规律提炼
  - desc: Scheduled `tianlu-evolution` monthly nudge memory for `cwd=/Users/luxiangnan/.openclaw/workspace-tianlu`, covering the `periodic_nudge.py monthly` entrypoint, the plain-text auto-delivery constraint, and verification of `memory/monthly-YYYY-MM.md`. Search this first when a task says `每月自省`, references the monthly nudge script, or asks for a maintenance-style reflection rather than an interactive writeup.
  - learnings: The represented run kept the cron contract tight: restore local context, read the skill spec, run `python3 /Users/luxiangnan/.openclaw/skills/tianlu-evolution/scripts/periodic_nudge.py monthly`, keep the response plain text, and verify `memory/monthly-2026-05.md` on disk. The useful retrieval clue is the emitted summary shape itself: `本月无重大规律提炼`, `活跃 Skills：0 个`, and a suggestion to enable the cron periodic nudge.

#### 2026-04-30

- 每日总结与 self_evolution 回写: 每日总结, 明日首要任务, trade_journal.py, self_evolution_2026_q1.md, memory/2026-04-30.md, 179笔, 胜率43.6%, 总PnL$213.52
  - desc: End-of-day summary memory for `cwd=/Users/luxiangnan/.openclaw/workspace-tianlu`, covering how to pull the latest trading stats, append a short `今日总结（200字内）+ 明日首要任务` to `memory/YYYY-MM-DD.md`, update `memory/learning/self_evolution_2026_q1.md`, and verify both writes on disk. Search this first when a task mentions `每日总结`, `明日首要任务`, `self_evolution_2026_q1.md`, or asks to回写今日总结.
  - learnings: The freshest validated run pulled the latest `trade_journal.py` stats first, moved the day-end baseline to `总179笔 | 胜率43.6% | 总PnL$213.52`, noted new `ETH` 3x long and `BTC` 5x short positions, then wrote both `memory/2026-04-30.md` and `self_evolution_2026_q1.md`. The durable closure rule is double writeback plus `tail` / `grep` / `stat`; do not reuse older figures like `161笔 / $436.15`.

- even2026 / Even G2 bridge diagnostics: even2026.tianlu2026.org, G2 Bridge, Even G2, ocuclaw, openclaw-gateway, localhost:18789, localhost:9000, tunnel.yml, .cloudflared/config.yml, openclaw.json, openclaw.plugin.json, even-ai-settings.json, MiniMax-M2.7-highspeed, api.minimaxi.com/v1/responses, codex app-server startup aborted, native hook relay not found, Unauthorized
  - desc: Live OpenClaw host diagnostics for `cwd=/Users/luxiangnan/.openclaw/workspace-tianlu`, covering direct answers about the current model, Mac runtime health, and whether the `even2026` domain service and Even G2 bridge/plugin are really healthy end to end. Search this first when a task mentions `even2026`, `Even G2`, `ocuclaw`, `G2 Bridge`, or asks “你现在是什么模型？” / “电脑的状态如何？” / “正常吗”.
  - learnings: The validated route for this diagnostic family is layered, not single-surface: `session_status` answered the model directly as `openai-codex/gpt-5.5`; the Mac health check was concise but evidence-backed via `uptime`, `pmset -g batt`, `vm_stat`, `df -h`, `top`, and `memory_pressure`, with `mds_stores` as the main transient CPU load; and the `even2026` bridge check required comparing the public domain, local listeners, plugin config, runtime Even AI settings, and logs together. The public domain and `localhost:18789` were healthy, `9000` listening with `404` on HTTP paths was expected relay behavior, `plugins.entries.ocuclaw` was enabled with `evenAiEnabled`, but a later live repair showed that `~/.openclaw/even-ai-settings.json` could still route glasses traffic to `MiniMax-M2.7-highspeed` and trigger `unexpected status 404 Not Found ... api.minimaxi.com/v1/responses`; changing that runtime file to `openai-codex/gpt-5.5` with think `medium` was the new high-value routing clue. The log lines `codex app-server startup aborted` and `native hook relay not found` still mean “domain up” does not prove the actual G2 request path is healthy.

- internal heartbeat reminders and daily logs: HEARTBEAT.md, HEARTBEAT_OK, pending_approvals.json, aggregator.py, ../monitor_sentiment.py, news_crawler.py, trade_journal.py, market_crawler.py, sentiment_latest.json, memory/2026-04-29.md, memory/2026-04-30.md
  - desc: Repeated internal reminder handling for `cwd=/Users/luxiangnan/.openclaw/workspace-tianlu`, covering the local `HEARTBEAT.md` policy, approvals checks, scheduled reminder commands, and the expected dated-memory append. Search this first when a task mentions `HEARTBEAT_OK`, `HEARTBEAT.md`, the local skill `skills/tianlu-heartbeat-reminders/SKILL.md`, or recurring reminder scripts such as `aggregator.py`, `news_crawler.py`, and `trade_journal.py`.
  - learnings: The reminder policy here is explicit: keep routine runs internal and do not relay them to the user unless requested. The freshest reminder evidence added two routing guardrails: if the instruction says “Do not infer or repeat old tasks from prior chats,” keep the run self-contained, and if it specifies `/Users/luxiangnan/.openclaw/workspace-tianlu/HEARTBEAT.md`, use that exact file rather than alternate docs paths. The proven mechanical path is `HEARTBEAT.md` -> `pending_approvals.json` check -> run the named script -> confirm `memory/YYYY-MM-DD.md`. The represented qintianjian/trade-journal verification remained concrete and stable: `aggregator.py && ../monitor_sentiment.py` repeatedly refreshed `/Users/luxiangnan/.sentiment_latest.json`, which remains the authoritative latest sentiment artifact and stayed at `做空/恐慌` / `SHORT` / `confidence=68`; `news_crawler.py` refreshed `/Users/luxiangnan/edict/data/sentiment_pool.json`, which should be parsed via `news.items` and kept returning `count=20` with valid JSON; and the late-day `trade_journal.py` reruns showed that once the summary stabilizes at `总177笔 | 胜率42.9% | 总PnL$208.51`, the cheap useful proof is timestamp freshness plus `python3 -m json.tool`, not re-reading every position line.

### /Users/luxiangnan/Desktop/🧠 天禄记忆库

#### 2026-04-30

- V6.5 交易治理与 AI 权限边界: V6.5, AI权限边界, 审核员, 管理员, 乱平乱入场, signal quality, 噪音过大, S/R撑压判断错误, 资金净流入流出, WindowServer watchdog, JetsamEvent, bot_manager.sh, /health, /api/v1/ping
  - desc: Trading-system governance memory for `cwd=/Users/luxiangnan/Desktop/🧠 天禄记忆库` with linked execution code under `/Users/luxiangnan/freqtrade_console` and `/Users/luxiangnan/freqtrade`, covering crash/resource triage, trading-stack restoration, V6.5 hardening, and the durable rule that AI should audit/administer signals rather than trade autonomously. Search this first when a task mentions `V6.5`, `AI权限边界`, `乱平乱入场`, `signal quality`, or asks to stabilize and restart the trading stack without changing the core rule set.
  - learnings: The freshest supported memory says the core problem was not V6.5 itself but “让AI去全权负责”. The recovery path preserved `VPN` / `TODESK` / `OPENCLAW` / `VSCODE`, treated `WindowServer watchdog + JetsamEvent` as memory-pressure evidence, restored services with `bot_manager.sh`, and verified the stack through `9090-9097` plus `9099` health endpoints rather than `ps` alone. The durable architecture rule is now explicit: `V6.5 = hard logic`, `robot = executor`, `天眼/出山AI = 审核员和管理员`; improvement should target `噪音过大`, `S/R撑压判断错误`, and net-flow accuracy before any broader autonomy is reconsidered.

### Older Memory Topics
